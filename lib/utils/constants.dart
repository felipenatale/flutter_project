import 'package:flutter/material.dart';

// ICONS
const nextIcon = Icon(Icons.navigate_next);
const iconAdd = Icon(Icons.add);

// STYLES
const padding = EdgeInsets.symmetric(horizontal: 8);
const paddingButton = EdgeInsets.only(top: 8);

// WIDGETS
const divider = Divider(
  height: 2,
  color: Colors.black12,
);

const buttonLabel = Text("Salvar");

//Tela Registro Entrada

const paddingRegistroPonto = EdgeInsets.all(16.0);
const titleScreanResgister = "Registro Ponto";
const textStyletitleScreanResgister = TextStyle(fontSize: 18, fontWeight: FontWeight.bold);

const subtitleCondominium = "Condomínio";
const textStyleSubtitleCondominium = TextStyle(fontSize: 24);
const textAlingSubtitleCondominium = TextAlign.left;

const paddingInputDecorationDropDown = EdgeInsets.symmetric( vertical: 15, horizontal: 20);
final borderRadiusAllScreen = BorderRadius.circular(10.0);

const titleButtonRegister = "Entrada";
const colorElevatedButtonRegister = Colors.blue;
const paddingElevatedButtonRegister = EdgeInsets.symmetric(horizontal: 10, vertical: 15);